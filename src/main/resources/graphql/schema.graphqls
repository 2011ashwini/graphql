type Query {
    getUser(id: String!): User
    bookById(id: ID): Book
}

type User {
    id: String,
    lastName: String
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}